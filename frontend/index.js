/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// Use a hardcoded app name for web compatibility
const appName = 'RindwaApp';

AppRegistry.registerComponent(appName, () => App);